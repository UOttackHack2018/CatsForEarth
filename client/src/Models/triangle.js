class Triangle{
    constructor(x, y, base, height){
        this.base = base;
        this.height = height;
        
        this.area = (base * height) / 2();
        }   
}

export default Triangle;
